# Core dependencies
numpy>=1.21.0
scipy>=1.7.0
trimesh>=3.10.0
pyyaml>=6.0

# Visualisation (used by scripts/*.py for figure regeneration)
matplotlib>=3.5.0
pillow>=9.0.0

# Baseline used in Section VI (CMA-ES)
cma>=4.0.0

# For mesh boolean operations (optional but recommended)
# manifold3d>=2.0.0  # or use blender backend
# open3d>=0.15.0
